function loadWebsite(){
    const content = document.getElementById("content")

    // const midBox = document.createElement("div");
    // midBox.classList.add("midBox")

    // const mainSpace = document.createElement("div");
    // mainSpace.textContent = "sample";
    // mainSpace.classList.add("main");

    // const sidebar = document.createElement("div");
    // sidebar.classList.add("sidebar");
    // const projects = document.createElement("div");
    // const addButton = document.createElement("button");
    // projects.classList.add("project");
    // addButton.classList.add("addButton");
    // projects.textContent = "Projects";
    // addButton.textContent = "Add Project";
    // sidebar.appendChild(projects);
    // sidebar.appendChild(addButton);

    // midBox.appendChild(sidebar);
    // midBox.appendChild(mainSpace);

    // const header = document.createElement("div");
    // const title = document.createElement("div");
    // title.classList.add("title");
    // title.textContent = "To-Do List";
    // header.appendChild(title);

    // content.appendChild(header);
    // content.appendChild(midBox);

}

export default loadWebsite;